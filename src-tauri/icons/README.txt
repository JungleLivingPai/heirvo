Place app icons here:
- 32x32.png
- 128x128.png
- 128x128@2x.png
- icon.icns (macOS)
- icon.ico (Windows)

Until you supply real icons, run:
  cargo tauri icon path/to/source.png

That command (after `cargo install tauri-cli`) generates all required sizes.
